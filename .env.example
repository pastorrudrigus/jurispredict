# Connection string Postgres do Supabase — usar uma role READ-ONLY dedicada,
# nunca o usuário postgres master (Seção 7 da spec).
# Formato: postgresql://usuario_readonly:senha@db.<ref>.supabase.co:5432/postgres
SUPABASE_DB_URL=
