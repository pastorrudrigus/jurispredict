{
  "name": "crivo",
  "version": "0.1.0",
  "private": true,
  "description": "Crivo — motor de decisão de crédito da JC Distribuidora. Consome o bureau Ábaco e o histórico do ERP para decidir score, rating e limite.",
  "type": "module",
  "scripts": {
    "audit": "tsx scripts/audit-db.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
